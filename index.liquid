---
layout: layout.liquid
title: "Planetary Playing Cards"
---

<h1>Planetary Playing Cards</h1>

<section class="grid">
    <article class="card sun">
        <div class="sun-avatar"></div>
        <h2>Sun</h2>
        <p>Type G2V</p>
    </article>

    {% for planet in planets.planets %}
        {% include "planet-card.liquid" %}
    {% endfor %}
</section>
